export function takeTurn () {
  
}

export function generateMap (seed: number, floor: number): [boolean][] {
  return [];
}

export function aStar (start: [number, number], end: [number, number], map: [[boolean]]): [number, number][] {
  return [];
}